<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<!--1.将这里的父模块由springframework改为我们的父模块-->
	<parent>
		<groupId>com.lzp</groupId>
		<artifactId>demo</artifactId>
		<version>0.0.1-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath><!--父pom路径-->
	</parent>

	<!--2.groupId和version继承父模块，可以删除，-->
	<artifactId>test2</artifactId>
	<name>test2</name>
	<description>Demo project for Spring Boot</description>

	<!--3.properties会继承父模块中的配置，这里也可以删除-->
	<!--	<properties>-->
	<!--		<java.version>1.8</java.version>-->
	<!--	</properties>-->

	<!--4.由于可以继承父模块中的依赖，这里可以删除-->
	<dependencies>
	</dependencies>

	<!--5.如果没有使用特殊的插件，这里可以直接继承父pom中的插件-->
	<build>
		<plugins>
			<!--			<plugin>-->
			<!--				<groupId>org.springframework.boot</groupId>-->
			<!--				<artifactId>spring-boot-maven-plugin</artifactId>-->
			<!--			</plugin>-->
		</plugins>
	</build>

</project>